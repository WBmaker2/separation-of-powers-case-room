# 초등학생 실사용 점검 및 개선 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development while implementing this plan task-by-task.

**작성일:** 2026. 7. 18.  
**목표:** 초등학생이 모바일과 데스크톱에서 안내를 놓치지 않고, 어려운 표현 때문에 흐름이 끊기지 않으며, 선택 결과를 바로 이해할 수 있도록 개선합니다.  
**구조:** 현재의 `CaseRoom` 상태 흐름과 3개 가상 사건 데이터는 유지합니다. 오답 시도 상태만 별도로 기록해 해당 선택지 안에 안내를 붙이고, 문구와 반응형 스타일을 작은 범위에서 다듬습니다.  
**기술:** React 19, TypeScript, CSS, Node test runner, Playwright 실사용 점검

## 1. 실사용 점검 조건

- 사용자 가정: 사회 교과에서 삼권분립을 처음 배우는 초등학생
- 화면: 모바일 390×844, 데스크톱 화면
- 점검 흐름: 시작 → 기관 안내 → 사건 소개 → 선택지 오답/정답 → 사건 기록 → 이유 정리
- 확인 항목: 문장 난이도, 다음 행동의 명확성, 눌렀을 때 결과의 즉시성, 글자 크기, 하단 진행표와 콘텐츠의 관계, 접근성 안내
- 기준 화면: [모바일 오답 안내 개선 전 화면](artifacts/qa/2026-07-18-mobile-wrong-feedback-before.png)

## 2. 발견한 문제와 근거

### P0 — 첫 번째 오답의 안내가 화면 밖에 생김

모바일 사건 2의 두 번째 단계에서 첫 번째 선택지 `법원 — 예산안을 국회에 제출해요`를 눌렀습니다. 선택지의 화면 위치는 세로 736~844px였지만, 안내 문장은 세 선택지 아래 1,098px부터 나타났습니다. 현재 화면 아래로 약 250px 떨어져 있어 학생은 눌러도 아무 반응이 없다고 느낄 수 있습니다.

개선 기준:

- 오답 안내를 누른 카드 안에 바로 표시합니다.
- 카드 테두리와 작은 `다시 생각해요` 표시로 상태를 시각적으로 구분합니다.
- 화면 낭독기용 `aria-live` 안내는 유지합니다.
- 다른 선택지를 누르면 이전 오답 표시를 지우고 새 시도만 보여 줍니다.

### P1 — 일부 문장이 초등학생에게 추상적이거나 문어체임

| 현재 표현 | 어려운 점 | 개선 표현 |
|---|---|---|
| 사건 상태가 어떻게 바뀌는지 | `상태`가 추상적임 | 사건이 어떻게 달라지는지 |
| 이름이나 학번을 받지 않으며 | 문어체 어미 | 이름이나 학번은 쓰지 않아요 |
| 다른 기관의 검토가 어디서 들어오는지 | `검토가 들어오다`가 어색함 | 선택 뒤에 사건이 어떻게 달라지는지 |
| 역할과 한계 | `한계`가 첫 안내에서 추상적임 | 하는 일과 하지 않는 일 |
| 정책 찬반이나 기관의 승패 | 교과서식 명사 나열 | 어떤 생각이 맞는지, 어느 기관이 이겼는지 |
| 다음에 필요한 절차 | `절차`가 반복됨 | 다음에 할 일 |
| 점수 대신 관계 | `관계`가 추상적임 | 점수 없이 역할 |
| 구체적인 다툼과 행정처분의 위법 여부 | 한 문장에 어려운 명사가 많음 | 사람이나 단체의 다툼, 행정기관의 결정이 법에 맞는지 |
| 재검토 또는 독립된 판단 | 압축된 법률 표현 | 다른 기관이 다시 살피거나 법원이 지시 없이 판단 |
| 독립된 검토가 가능해져요 | 결론 제목이 어려움 | 서로 다시 살필 수 있어요 |

### P1 — 이유 정리 화면이 숨은 정답 찾기처럼 보임

화면 제목은 `모두 선택해요`라고 쓰여 있지만, 세 선택지가 모두 맞는 설명이라는 사실이 선택 전에 명확하지 않습니다. 초등학생은 틀린 선택지를 피해야 하는 퀴즈로 오해할 수 있습니다.

개선 기준:

- `세 가지 모두 맞는 설명이에요. 하나씩 눌러 확인해요.`를 제목 바로 아래에 표시합니다.
- 기존 0/3 진행 표시와 완료 조건은 유지합니다.

### P2 — 모바일 보조 글자의 크기가 작음

- 헤더의 `공식 근거`, `업데이트 내역`은 12px입니다.
- 하단 5단계 진행표는 10px입니다.

개선 기준:

- 헤더 버튼을 13px, 진행표를 11px로 올립니다.
- 390px 화면에서 가로 넘침이 없어야 합니다.
- 콘텐츠 마지막 부분이 고정 진행표에 가리지 않도록 모바일 아래 여백을 넉넉히 둡니다.

### P2 — 브라우저 콘솔에 favicon 404가 남음

`/favicon.ico` 요청이 404로 기록됩니다. 학습 기능에는 영향이 없지만 공개 앱의 기본 품질 신호이므로 기존 SVG 파비콘을 레이아웃 메타데이터에 명시해 요청 오류를 없앱니다.

## 3. 접근 방법 비교

### A. 문구 개선 + 선택지 안의 즉시 안내 + 모바일 가독성 보정 — 채택

- 장점: 실제로 확인된 문제를 직접 해결하며 기존 사건 구조와 학습 목표를 보존합니다.
- 장점: 코드 변경 범위가 작아 기존 3개 사건과 정답 분산 규칙의 회귀 위험이 낮습니다.
- 단점: 사건 자체의 법률 용어는 교과 목표 때문에 완전히 없앨 수 없고 쉬운 풀이와 함께 사용해야 합니다.

### B. 문구만 쉽게 바꾸기

- 장점: 가장 빠르고 위험이 낮습니다.
- 단점: 오답 안내가 화면 밖에 생기는 핵심 기능 문제를 해결하지 못합니다.

### C. 선택지를 한 장씩 보여 주는 단계형 화면으로 재설계

- 장점: 한 번에 읽는 양이 줄어듭니다.
- 단점: 세 선택지를 비교해 기관 역할을 구분하는 현재 학습 활동이 약해지고, 전체 흐름과 테스트를 크게 다시 만들어야 합니다.

**결정:** A를 적용합니다. 법률 개념의 정확성은 유지하되, 화면의 안내 문장과 반응은 초등학생이 한 번에 이해할 수 있는 방식으로 바꿉니다.

## 4. 구현 계획

### 작업 1 — 실패하는 회귀 테스트 추가

**수정 파일:** `tests/rendered-html.test.mjs`

- 서버 렌더링 시작 화면에 `사건이 어떻게 달라지는지`, `이름이나 학번은 쓰지 않아요`가 보이는지 검사합니다.
- 예전 어려운 표현 `사건 상태가`, `받지 않으며`가 사라졌는지 검사합니다.
- `CaseRoom.tsx`가 오답 시도 ID를 관리하는지 검사합니다.
- `CaseBoard.tsx`가 선택지 안의 오답 안내와 `aria-describedby`를 제공하는지 검사합니다.
- 이유 정리 도움말과 2026. 7. 18. 업데이트 내역을 검사합니다.

실행: `npm run test:rendered`  
예상: 구현 전에는 새 문구와 구조가 없어 실패합니다.

### 작업 2 — 오답 안내를 선택지 안으로 이동

**수정 파일:** `app/case-room/CaseRoom.tsx`, `app/case-room/CaseBoard.tsx`, `app/case-room/case-room-board.css`

- `attemptedActionId` 상태를 추가합니다.
- 정답 선택, 단계 이동, 되돌리기, 다음 사건, 처음부터 다시 시작할 때 오답 시도를 초기화합니다.
- 오답 카드에 `needs-review` 클래스를 적용합니다.
- 카드 본문에 `다시 생각해요`와 해당 피드백을 표시합니다.
- 전역 피드백 영역은 정답 선택 안내와 화면 낭독기 알림만 담당하도록 정리합니다.
- 오답 카드의 색상은 정답 선택 색상과 구분되는 주황 계열을 사용합니다.

### 작업 3 — 초등학생용 문구로 조정

**수정 파일:** `app/case-room/IntroScreens.tsx`, `app/case-room/CaseRoom.tsx`, `app/case-room/CaseBoard.tsx`, `app/case-room/SummaryScreens.tsx`, `app/case-room/content.ts`

- 2절 표의 표현을 반영합니다.
- 교과 개념어 `편성`, `심의·의결`, `행정처분`, `위법`은 사건과 용어 도움말에서 유지합니다.
- 이유 정리 화면에 세 설명이 모두 맞다는 안내를 추가합니다.
- 업데이트 내역 맨 앞에 2026. 7. 18. 개선 기록을 추가합니다.

### 작업 4 — 모바일 가독성과 콘솔 품질 개선

**수정 파일:** `app/case-room/case-room-responsive.css`, `app/layout.tsx`

- 모바일 헤더 버튼 13px, 진행표 11px로 조정합니다.
- 앱 아래 여백을 고정 진행표보다 충분히 크게 유지합니다.
- 기존 SVG 파비콘을 메타데이터 아이콘으로 연결합니다.

### 작업 5 — 전체 자동 검증

실행:

1. `npm test`
2. `npm run lint`
3. 변경 파일별 500줄 미만 확인

통과 기준:

- 사건 도메인 테스트와 정답 위치 4/4/4 규칙이 유지됩니다.
- 빌드와 서버 렌더링 테스트가 통과합니다.
- 린트 오류가 없습니다.
- 모든 `app/case-room` TS/TSX/CSS 파일이 500줄 미만입니다.

### 작업 6 — 초등학생 관점 브라우저 재검증

모바일 390×844:

- 첫 번째 오답을 누르면 같은 카드 안에서 안내가 바로 보입니다.
- 정답을 누르면 오답 표시가 사라지고 `선택됨`이 보입니다.
- 헤더와 하단 진행표가 가로로 넘치지 않습니다.
- 시작 화면의 쉬운 문구가 자연스럽게 줄바꿈됩니다.

데스크톱:

- 세 선택지가 한 줄에서 고르게 보입니다.
- 오답 카드의 안내가 카드 높이를 늘려도 주변 레이아웃이 깨지지 않습니다.
- 이유 정리 도움말과 업데이트 내역이 보입니다.
- 콘솔에 앱 오류와 favicon 404가 없습니다.

## 5. 완료 기준

- 이 문서가 구현보다 먼저 저장되어 있습니다.
- 오답 안내 위치, 어려운 문장, 이유 정리 오해 가능성, 모바일 글자 크기를 코드에서 개선했습니다.
- 2026. 7. 18. 업데이트 내역이 앱에서 확인됩니다.
- 자동 테스트·린트·빌드와 모바일/데스크톱 브라우저 실사용 검증이 모두 통과합니다.
- 이번 요청에는 공개 재배포가 포함되지 않으므로 로컬 구현과 검증까지 완료합니다.

## 6. 구현 및 검증 결과

### 적용 결과

- 첫 오답 안내를 선택지 안에 넣고 `다시 생각해요` 상태를 추가했습니다.
- 오답 설명이 생기면 해당 설명을 화면 중앙으로 이동하도록 해 모바일에서도 즉시 보이게 했습니다.
- 정답을 다시 고르면 오답 상태가 사라지고 `✓ 선택됨` 상태로 바뀌는 것을 확인했습니다.
- 시작, 기관 안내, 사건 소개, 상태 카드, 사건 정리, 법원·헌법재판소 구분, 이유 정리, 최종 화면의 어려운 문장을 조정했습니다.
- 이유 정리에 `세 가지 모두 맞는 설명`이라는 안내를 추가했습니다.
- 모바일 헤더는 13px, 하단 진행표는 11px로 높이고 화면 아래 여백을 늘렸습니다.
- 2026. 7. 18. 업데이트 내역과 SVG 파비콘 메타데이터를 추가했습니다.

### 자동 검증

- `npm test`: 도메인 11개, 서버 렌더링·구조 2개 테스트 통과, 빌드 성공
- `npm run lint`: 통과
- 정답 위치: 왼쪽·가운데·오른쪽 각각 4회 유지
- 파일 크기: 가장 긴 소스 파일 423줄로 모든 파일이 500줄 미만

### 브라우저 재검증

- 모바일 390×844: 가로 넘침 없음 (`scrollWidth 375 = clientWidth 375`)
- 오답 설명: 개선 전 세로 1,098px로 화면 밖 → 개선 후 391~449px로 하단 진행표 위에 완전히 표시
- 모바일 보조 글자: 헤더 13px, 진행표 11px 확인
- 데스크톱 1440×900: 선택지 3개가 각각 365px 너비로 한 줄에 유지되고 가로 넘침 없음
- 전체 3개 사건, 법원·헌법재판소 안내, 이유 3개 선택, 최종 화면까지 실제 클릭 완료
- 브라우저 콘솔: 오류 0개, 경고 0개

### 화면 증거

- [개선 전 — 첫 오답 안내가 화면 밖에 있음](artifacts/qa/2026-07-18-mobile-wrong-feedback-before.png)
- [개선 후 — 같은 카드 안에 오답 안내가 보임](artifacts/qa/2026-07-18-mobile-wrong-feedback-after.png)
