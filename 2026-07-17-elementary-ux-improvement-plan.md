# 초등학생 사용성 개선 구현 계획

## 배경

초등학교 5~6학년 사용성을 가정한 전체 흐름 테스트에서 기능은 완주됐지만, 행동 주체와 기관이 섞여 보이는 표기, 어려운 법률 용어, 첫 화면 아래로 밀린 선택지, 추상적인 버튼 문구, 이전 단계 부재가 확인됐다.

## 구현 목표

1. `법원 · 영향을 받은 가상 단체`처럼 오개념을 만들 수 있는 표기를 없앤다.
2. 사건마다 어려운 말을 쉬운 문장으로 바로 확인할 수 있게 한다.
3. 사건 화면의 제목·기록·현재 상태 높이를 줄여 선택지가 더 빨리 보이게 한다.
4. `처리 적용`과 딱딱한 피드백을 초등학생이 행동 결과를 예상할 수 있는 말로 바꾼다.
5. 기관 안내와 사건 처리에서 이전 단계로 돌아가 선택을 다시 살펴볼 수 있게 한다.

## 세부 설계

### 행동 주체 표시

- 기관과 구체적 주체가 같으면 한 번만 표시한다.
- 정부·대통령·행정기관처럼 상위 기관 설명이 필요한 경우에만 `행정부 · 대통령`처럼 표시한다.
- 소송을 내는 가상 단체는 기관 카드가 아니라 `행동 주체 · 가상 단체`로 표시하고 중립 표식을 사용한다.
- 사건 처리 기록에는 추상적인 기관 분류 대신 실제 행동 주체를 남긴다.

### 쉬운 용어 도움말

- 사건별로 3~4개의 핵심 용어와 쉬운 풀이를 둔다.
- 사건 소개와 처리 화면에서 `어려운 말 쉽게 보기`를 펼쳐 확인할 수 있게 한다.
- 도움말은 키보드와 스크린리더로도 사용할 수 있는 기본 `details/summary` 구조로 만든다.

### 화면 밀도와 행동 발견성

- 사건 처리 화면의 큰 제목, 타임라인 여백, 현재 상태 카드 높이를 줄인다.
- 선택 영역의 제목을 짧고 직접적인 문장으로 유지한다.
- 데스크톱 첫 화면에서 현재 상태와 선택 영역의 시작이 함께 보이는 것을 목표로 한다.

### 이전 단계와 쉬운 문구

- 기관 안내에 `이전으로`를 제공한다.
- 사건 처리 기록이 있으면 `직전 단계로 돌아가기`를 제공한다.
- 사건 완료 화면에서도 마지막 선택을 다시 고칠 수 있게 한다.
- `처리 적용`은 `선택한 행동으로 진행하기`로 바꾸고, 선택 뒤 안내도 쉬운 말투로 통일한다.

## 검증 기준

- 기존 정답 위치 분포 `4 · 4 · 4`를 유지한다.
- 되돌리기 뒤 현재 상태와 기록이 정확히 한 단계 전으로 복원된다.
- 외부 참여자인 가상 단체가 법원 소속처럼 표시되지 않는다.
- 용어 도움말과 이전 버튼을 키보드로 사용할 수 있다.
- 단위 테스트, 빌드, 정적 렌더링 테스트, 린트를 통과한다.
- 브라우저에서 전체 학습 흐름, 오답, 예측, 되돌리기, 완료 화면 재진입을 확인한다.
- 화면 변경을 `업데이트 내역`에 기록한다.
