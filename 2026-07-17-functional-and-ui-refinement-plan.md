# 삼권분립 사건 처리실 기능·UI 개선 계획

- 작성일: 2026-07-17
- 대상: 초등학교 5~6학년 학습자
- 범위: 기존 가상 사건 3개와 중립적인 학습 구조는 유지하고, 진행 이해·선택 피드백·모바일 가독성을 개선한다.

## 1. 분석 방법

- React 컴포넌트, 상태 전이 엔진, 반응형 CSS와 접근성 속성을 점검했다.
- 데스크톱 1280×720과 모바일 390×844 화면에서 시작 화면부터 첫 사건 선택 화면까지 직접 사용했다.
- 기존 단위 테스트 11개, 렌더링 테스트 2개와 프로덕션 빌드가 통과하는 것을 기준선으로 확인했다.

## 2. 발견한 개선점

### A. 기능과 학습 흐름

1. 하단 진행표에 `기관 안내`가 없어, 안내 화면에서도 `사건 1`이 현재 단계처럼 보인다.
2. 행동을 고르기 전이나 결과 예측을 마치기 전, 진행 버튼이 비활성화된 이유가 화면에 분명하게 나오지 않는다.
3. 마지막 이유 정리에서 세 항목을 모두 골라야 하지만, 현재 몇 개를 골랐는지 바로 알기 어렵다.

### B. 피드백과 선택 상태

1. 알맞은 행동을 고른 안내와 다시 생각해 볼 행동의 안내가 같은 주황색으로 보여 의미가 구분되지 않는다.
2. 선택된 행동 카드는 배경색만 달라져, 색 구분이 어려운 학생에게 선택 상태가 충분히 선명하지 않다.

### C. 디자인과 모바일 UI

1. 작은 화면에서 긴 제목이 낱말 중간에서 끊겨 읽기 흐름이 어색하다.
2. 모바일 버튼 묶음에 `column-reverse`가 적용되어 화면에 보이는 순서와 키보드 이동 순서가 다르다.
3. 모바일 사건 화면에서 현재 상태 카드가 높아 첫 번째 행동 선택지가 첫 화면 아래로 많이 밀린다.
4. 하단 진행표의 현재 단계가 시각적으로만 표현되어 보조기기에는 현재 위치가 충분히 전달되지 않는다.

## 3. 구현 범위

### 진행표 개선

- `안내 → 사건 1 → 사건 2 → 사건 3 → 정리`의 5단계로 표시한다.
- 완료 단계에는 체크 표시를, 현재 단계에는 `aria-current="step"`을 제공한다.

### 선택 안내 개선

- 행동 카드에 `선택됨` 표시를 추가한다.
- 알맞은 절차 선택은 초록 계열, 다시 생각할 선택은 주황 계열 안내로 구분한다.
- 진행 버튼 위 문구를 `행동 선택 필요`, `결과 예상 필요`, `진행 준비 완료` 상태에 맞게 바꾼다.

### 이유 정리 개선

- `0/3`, `1/3`처럼 찾은 이유의 수를 실시간으로 표시한다.
- 세 이유를 모두 찾았을 때 완료 안내를 제공한다.

### 반응형·접근성 개선

- 한글 제목과 버튼 문구가 낱말 중간에서 끊기지 않도록 줄바꿈 규칙을 보완한다.
- 모바일 버튼의 시각 순서와 DOM 순서를 일치시킨다.
- 모바일 현재 상태 카드의 높이와 여백을 줄여 첫 행동 선택지를 더 빨리 볼 수 있게 한다.

### 업데이트 기록

- 앱의 `업데이트 내역`에 2026-07-17 기능·UI 개선 항목을 추가한다.

## 4. 검증 계획

- 단위 테스트와 렌더링 테스트에 새 진행 안내·선택 안내 문구를 추가한다.
- `npm test`, `npm run lint`를 실행한다.
- 실제 브라우저에서 데스크톱과 모바일 화면을 확인한다.
- 시작 → 기관 안내 → 사건 소개 → 행동 선택 → 예측 → 사건 완료 흐름을 직접 진행한다.
- 진행표의 현재 단계, 선택 상태, 안내 색상, 버튼 활성화 조건, 모바일 가로 넘침과 콘솔 오류를 확인한다.

## 5. 완료 기준

- 학생이 현재 단계와 다음에 해야 할 일을 설명 없이도 화면에서 찾을 수 있다.
- 색만 보지 않아도 선택 상태와 진행 가능 여부를 알 수 있다.
- 모바일에서 제목이 낱말 중간에 끊기지 않고, 키보드 이동 순서와 화면 순서가 일치한다.
- 기존 사건 전이 규칙과 정답 위치 분산이 유지되며 모든 자동 검증이 통과한다.
